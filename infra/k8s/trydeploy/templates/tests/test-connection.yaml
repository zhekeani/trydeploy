apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "trydeploy.fullname" . }}-test-connection"
  labels:
    {{- include "trydeploy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "trydeploy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
